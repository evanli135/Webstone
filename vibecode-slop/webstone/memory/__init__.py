"""Memory layer — graph store, vector store, and unified retrieval.

The memory layer is part of the cognitive side: it stores and retrieves
knowledge accumulated during research runs. It is decoupled from the
execution runtime via Protocol interfaces.
"""
